server:
  port: 9935    
spring:
  banner:    location: classpath:hisBanner.txt
  datasource:
    url: jdbc:h2:mem:HIS
    driver-class-name: org.h2.Driver
    username: HIS
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  mvc:
    view:
      prefix: /views/
      suffix: .jsp
  mail:
    host: smtp.gmail.com
    port: 587
    username: abc
    password: xyz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
